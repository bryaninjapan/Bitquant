# GitHub Pages 设置步骤

## ⚠️ 重要：必须先完成这些步骤

### 步骤 1: 启用 GitHub Pages

1. 访问你的仓库: https://github.com/bryaninjapan/Bitquant
2. 点击 **Settings** (设置)
3. 在左侧菜单找到 **Pages** (页面)
4. 在 **Build and deployment** 部分:
   - **Source**: 选择 **GitHub Actions** (不是 "Deploy from a branch")
   - 点击 **Save**

### 步骤 2: 推送修复后的工作流

推送代码后，GitHub Actions 会自动运行：

```bash
git push origin main
```

### 步骤 3: 检查部署状态

1. 访问 **Actions** 标签页
2. 查看最新的工作流运行
3. 等待部署完成（通常需要 1-2 分钟）

### 步骤 4: 访问你的网站

部署成功后，访问：
```
https://bryaninjapan.github.io/Bitquant/
```

## 🔍 如果仍然遇到错误

### 错误: "Cannot find any run"
- 确保 GitHub Pages 已启用（步骤 1）
- 等待几分钟后重试

### 错误: "404 Not Found"
- 检查 Settings → Pages 是否选择了 "GitHub Actions"
- 确保工作流文件在 `.github/workflows/` 目录下

### 错误: "Permission denied"
- 检查仓库设置中的 Actions 权限
- 确保工作流有 `pages: write` 权限

## 📝 注意事项

- 第一次部署可能需要 5-10 分钟
- 如果仓库是私有的，需要升级到 GitHub Pro 才能使用 GitHub Pages
- 部署后，网站更新可能需要几分钟才能生效

