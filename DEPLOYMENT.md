# 部署说明 (Deployment Guide)

## 📦 代码已提交到本地仓库

代码已经成功提交到本地 Git 仓库。现在需要推送到 GitHub 并配置自动部署。

## 🚀 推送到 GitHub

### 方法 1: 使用 HTTPS（需要 Personal Access Token）

1. 在 GitHub 上创建 Personal Access Token:
   - 访问: https://github.com/settings/tokens
   - 点击 "Generate new token (classic)"
   - 选择权限: `repo` (完整仓库权限)
   - 生成并复制 token

2. 推送代码:
```bash
git push origin main
# 当提示输入用户名时，输入你的 GitHub 用户名
# 当提示输入密码时，输入刚才生成的 Personal Access Token
```

### 方法 2: 使用 SSH

1. 检查是否已有 SSH 密钥:
```bash
ls -al ~/.ssh
```

2. 如果没有，生成新的 SSH 密钥:
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

3. 将公钥添加到 GitHub:
   - 复制公钥: `cat ~/.ssh/id_ed25519.pub`
   - 访问: https://github.com/settings/keys
   - 点击 "New SSH key"，粘贴公钥

4. 更改远程仓库地址为 SSH:
```bash
git remote set-url origin git@github.com:bryaninjapan/Bitquant.git
git push origin main
```

## 🌐 配置 GitHub Pages 部署

### 步骤 1: 启用 GitHub Pages

1. 访问你的仓库: https://github.com/bryaninjapan/Bitquant
2. 点击 **Settings** (设置)
3. 在左侧菜单找到 **Pages** (页面)
4. 在 **Source** 部分:
   - 选择 **GitHub Actions** 作为源
   - 保存设置

### 步骤 2: 配置 GitHub Secrets (可选，用于构建时使用真实 API Key)

如果你希望在构建时使用真实的 API Key（不推荐，因为会暴露在构建产物中），可以设置 Secret:

1. 在仓库页面，点击 **Settings** → **Secrets and variables** → **Actions**
2. 点击 **New repository secret**
3. 名称: `GEMINI_API_KEY`
4. 值: 你的 Gemini API Key
5. 点击 **Add secret**

**注意**: 由于这是前端应用，API Key 会在客户端使用。更好的做法是让用户在浏览器中配置 `.env.local` 文件，而不是在构建时硬编码。

### 步骤 3: 触发部署

推送代码到 `main` 分支后，GitHub Actions 会自动：

1. 安装依赖
2. 构建项目
3. 部署到 GitHub Pages

你可以在 **Actions** 标签页查看部署进度。

## 📍 访问部署的网站

部署完成后，你的网站将在以下地址可用:

```
https://bryaninjapan.github.io/Bitquant/
```

## 🔧 本地测试构建

在推送之前，你可以本地测试构建:

```bash
npm run build
npm run preview
```

## 📝 注意事项

1. **API Key 安全**: 
   - 不要将 `.env.local` 文件提交到 Git
   - 用户需要在浏览器中配置自己的 API Key
   - 或者使用环境变量（但会暴露在客户端代码中）

2. **GitHub Pages 限制**:
   - 免费账户有构建时间限制
   - 静态文件大小限制
   - 每月有带宽限制

3. **自定义域名** (可选):
   - 在 GitHub Pages 设置中可以配置自定义域名
   - 需要配置 DNS 记录

## 🆘 故障排除

### 如果 GitHub Actions 失败:

1. 检查 **Actions** 标签页中的错误日志
2. 确保 `package.json` 中的脚本正确
3. 检查 Node.js 版本是否兼容

### 如果页面显示 404:

1. 检查 GitHub Pages 设置是否正确
2. 确保 `vite.config.ts` 中的 `base` 路径正确
3. 等待几分钟让 GitHub Pages 更新

### 如果 API 调用失败:

1. 确保在浏览器中配置了正确的 `GEMINI_API_KEY`
2. 检查浏览器控制台是否有错误
3. 验证 API Key 是否有效

